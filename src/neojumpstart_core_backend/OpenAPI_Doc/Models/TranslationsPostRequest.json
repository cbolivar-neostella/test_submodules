{
    "type": "object",
    "required": [
        "lang",
        "key"
    ],
    "properties": {
        "lang": {
            "type": "string",
            "description": "language initials to create the custom translate"
        },
        "key": {
            "type": "string",
            "description": "path separated by dots where the translation belongs"
        }
    }
}